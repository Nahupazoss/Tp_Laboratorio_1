/*
 ============================================================================
 Name        : TRABAJO.c
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

#define TAM 10

typedef struct
{
	int id;
	char nombre[50];
	char apellido[50];
	float precio;
	int tipoPasajero;
	char codigoVuelo[4];
	int isEmpty;
}ePassenger;

int validarNumero(char numero[]);
int pedirEntero(char mensaje[],char mensajeError[]);
int menu();

int passenger_initArray(ePassenger *arrayPunteros[],int limite);

ePassenger* passenger_new(void);

ePassenger* passenger_newParametros(char *nombre,char *apellido,char *codigoVuelo,float precio,int id);

int main(void)
{
	setbuf(stdout,NULL);

	ePassenger *pasajero[TAM];

	int opcion;

	do
	{
		opcion = menu();

		switch(opcion)
		{
		case 1:

		break;

		case 2:

		break;

		case 3:

		break;

		case 4:
		break;

		case 5:
		break;

		case 6:
		break;

		case 7:
		break;

		case 8:
		break;

		case 9:
		break;

		case 10:
			printf("\nLeft seccsfull....\n");
		break;

		default:
			printf("\nWrong option....\n");
		break;
		}

	}while(opcion != 10);



	return EXIT_SUCCESS;
}
//////////////////////////////////////////////////////////////////////////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////////////////////////////////////
int validarNumero(char numero[])
{
    int retorno=1;
    int len;
    if(numero!=NULL){
        len=strlen(numero);
        for(int i=0;i<len;i++){ //"51"
            if(!isdigit(numero[i])){
                retorno=0;
                break;
            }
        }
    }
    return retorno;
}

int pedirEntero(char mensaje[],char mensajeError[])
{
    int retorno;
    char numero[1000];
    printf(mensaje);
    scanf("%s",numero);
    while(!validarNumero(numero)){
        printf(mensajeError);
        printf(mensaje);
        scanf("%s",numero);
    }
    retorno=atoi(numero);
    return retorno;
}

int menu()
{
	int opcion;

	  printf("\n=============================================MENU==================================================");
	opcion = pedirEntero("\n1〉〉Cargar los datos de los pasajeros desde el archivo data.csv (modo texto)"
			"\n2〉〉Cargar los datos de los pasajeros desde el archivo data.csv (modo binario)"
			"\n3〉〉Alta de pasajero"
			"\n4〉〉Modificar datos de pasajero"
			"\n5〉〉Baja de pasajero"
			"\n6〉〉Listar pasajeros"
			"\n7〉〉Ordenar pasajeros"
			"\n8〉Guardar los datos de los pasajeros en el archivo data.csv (modo texto)"
			"\n9〉〉Guardar los datos de los pasajeros en el archivo data.csv (modo binario)"
			"\n10〉〉Salir"
			"\n◉Opcion:","\nError, opcion no valida\n");
	  printf("===================================================================================================\n");

	  return opcion;
}

int passenger_initArray(ePassenger *arrayPunteros[],int limite)
{
	int retorno = -1;
	int i;

	if(arrayPunteros != NULL && limite > 0)
	{
		retorno=0;
		for(i=0;i<limite;i++)
		{
			arrayPunteros[i] = NULL;
		}
	}
	return retorno;
}

ePassenger* passenger_new(void)
{
	ePassenger *auxiliarP = NULL;

	auxiliarP = (ePassenger*)malloc(sizeof(ePassenger));

	return auxiliarP;
}

ePassenger* passenger_newParametros(char *nombre,char *apellido,char *codigoVuelo,float precio,int id)
{
	ePassenger *punteroPassenger = NULL;
	punteroPassenger = passenger_new();

	if(punteroPassenger != NULL)
	{
		strncpy(punteroPassenger->nombre,nombre,50);
		strncpy(punteroPassenger->apellido,apellido,50);
		strncpy(punteroPassenger->codigoVuelo,codigoVuelo,4);
		punteroPassenger->precio = precio;
		punteroPassenger->id = id;

	}
	return punteroPassenger;
}

